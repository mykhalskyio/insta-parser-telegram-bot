postgres:
  port: 5432
  host: pgdb
  user: postgres
  pass: qwerty123
  name: postgres
  sslmode: disable

telegram:
  token: token
  userparse: userparse # whose username must be parsed (without @)
  channel: channel # channel where the bot will send photos (without @)

instagram:
  username: username
  password: password

parser:
  minutes: 60 